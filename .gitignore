# ---------------------------------------------------------------------------
# Node / package managers
# ---------------------------------------------------------------------------
node_modules
.npm
.pnp.*
.yarn-integrity

# ---------------------------------------------------------------------------
# Logs & runtime diagnostics
# ---------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.pid
*.seed
*.pid.lock

# ---------------------------------------------------------------------------
# Build & bundle output
# ---------------------------------------------------------------------------
dist
dist-electron
out
build-output
.tmp
.cache
.parcel-cache
.turbo
.swc

# ---------------------------------------------------------------------------
# Test / coverage
# ---------------------------------------------------------------------------
coverage
.nyc_output

# ---------------------------------------------------------------------------
# TypeScript / lint caches
# ---------------------------------------------------------------------------
*.tsbuildinfo
.eslintcache
.stylelintcache

# ---------------------------------------------------------------------------
# Vite / Astro / framework caches
# ---------------------------------------------------------------------------
.vite
.astro

# ---------------------------------------------------------------------------
# Electron-builder packaging artifacts
#   - `release/` holds the final installers (.dmg, .exe, .AppImage, .deb).
#   - `build/icon.*` are regenerated by `scripts/prepare-build-icons.mjs`
#     from the source PNGs in `apps/desktop/src/renderer/src/asset/icon/`,
#     so they must NEVER be committed.
# ---------------------------------------------------------------------------
release/
build/icon.icns
build/icon.ico
build/icon.png

# Historical safety net: `vite-plugin-monaco-editor` once misresolved its
# output and produced a stray `apps/desktop/Users/<absolute>/...` tree.
# The bug is fixed in electron.vite.config.ts, but ignoring this path
# guards against regressions.
apps/desktop/Users/

# ---------------------------------------------------------------------------
# Secrets / local environment
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore

# ---------------------------------------------------------------------------
# Editor & OS junk
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# Misc local artifacts
# ---------------------------------------------------------------------------
*.tgz
stats.html
